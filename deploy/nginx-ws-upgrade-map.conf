# Подключите один раз внутри блока http { } в /etc/nginx/nginx.conf:
#   include /etc/nginx/snippets/agile-ws-upgrade-map.conf;
#
# Либо скопируйте содержимое вручную в http {}.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
